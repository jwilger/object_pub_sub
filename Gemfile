source 'https://rubygems.org'

# Specify your gem's dependencies in object_pub_sub.gemspec
gemspec

group :development do
  # Travis-ci seems to want this in the Gemfile and isn't picking it up from the
  # gemspec.
  gem 'rake'
end
